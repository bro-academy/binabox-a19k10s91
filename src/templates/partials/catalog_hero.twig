<section class="catalog-page">
    <h1 class="catalog-page__title">{{ catalog.hero.title }}</h1>
    <nav class="catalog-page__nav">
        <ul class="catalog-page__nav-list">
            {% for link in catalog.hero.links %}
            <li>
                <a href="{{ link.href }}" class="link link--tertiary catalog-page__nav-item {% if link.active %} catalog-page__nav-item--active {% endif %}">{{ link.label }}</a>
            </li>
              {% endfor %} 
        </ul>
    </nav>
</section>
