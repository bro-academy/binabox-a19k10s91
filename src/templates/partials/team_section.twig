<section class="team">
    <h2 class="section-title team__title">{{ index.team.title }}</h2>
    <ul class="team__list">
        {% for member in index.team.members %}
            {% include "../partials/personal_card.twig" %}
        {% endfor %}    
    </ul>
</section>