<div class="content-wrapper">
    <section class="team">
        <h2 class="team__title">{{ index.team.title }}</h2>
        <div class="team__wrapper">
            <ul class="team__list">
                {% for member in index.team.members %}
                    <li class="team__item personal-card">
                        {% include "../partials/personal_card.twig" %}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </section>
</div>