<div>
    <label for="{{ field.label }}" class="input-field-edit__label">
        {{ field.label }}
    </label>
    <textarea class="input-field-edit__field"
              name="{{ field.name }}"
              id="{{ field.name }}"
              placeholder="{{ field.placeholder }}"
              rows="8"
              required>
</textarea>
</div>