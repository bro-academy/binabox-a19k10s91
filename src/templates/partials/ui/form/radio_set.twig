<fieldset class="form-field">
    {% if field.label %}
    <legend class="form-field__label">{{ field.label }}</legend>
    {% endif %}
    {% for option in field.options %}
    {% include 'radio.twig' with {'field': field, 'option': option} %}
    {% endfor %}
</fieldset>
